# Prometheus configuration for Texnouz OCPP Central System

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "ocpp"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["ocpp:8080"]
        labels:
          service: "texnouz-ocpp"
