//! Domain services
//!
//! Domain services contain business logic that doesn't fit into a single entity.
//! Currently, the main business logic is handled at the application layer
//! (application::services). This module is reserved for pure domain services
//! that may be extracted in the future.